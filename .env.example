# Optional — LOCAL TESTING ONLY: jsDelivr URLs in dist/shell/manifest.json
# Uses this package's GitHub repo when you lack S3/CDN access on your machine.
# dev / beta / prod: Infra team publishes shell + assets to OpenForge CDN — do NOT use these vars there.
# MYBHARAT_SHELL_GITHUB_USER=
# MYBHARAT_SHELL_GITHUB_REPO=
# MYBHARAT_SHELL_GITHUB_TAG=v1.0.238

# ── Host app (CakePHP + Vite registration) — full template ──
# See docs/host-app.env.example (copy to your host project, not this package)
